package com.example.calculate.data

import com.example.calculate.model.Action

object DataSource {

    val actions = listOf(Action.SUM, Action.SUBTRACTION
        , Action.MULTIPLICATION, Action.DIVISION)
}